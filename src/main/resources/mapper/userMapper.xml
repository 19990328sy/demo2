<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserMapper">

    <select id="selectUserById" resultType="com.example.demo.model.User" parameterType="int">
        SELECT * from user where id = #{id}
    </select>
    <select id="selectUserByName" resultType="com.example.demo.model.User" parameterType="String">
        SELECT * from user where NAME = #{NAME}
    </select>
    <insert id="add" parameterType="com.example.demo.model.User">
        insert into  user (name,password) values(#{name},#{password})
    </insert>
    <!--<select id="login" resultType="com.example.demo.model.User" parameterType="String">
        SELECT * from user where NAME = #{NAME}
    </select>-->
    <select id="findByUsername" resultType="com.example.demo.model.User">
        SELECT * from user where NAME = #{NAME}
    </select>
    <select id="login" resultType="com.example.demo.model.User">
        select * from user where name =#{name} and password=#{password}
    </select>
    <insert id="regist" parameterType="com.example.demo.model.User">
        insert into  user (name,password) values(#{name},#{password})
    </insert>
</mapper>
